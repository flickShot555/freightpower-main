import { PulseCard, PulsePanel, UsersTable, CarriersTable } from '../admin/AdminShared';

// Re-export admin shared utilities from the super_admin folder so other
// super_admin components can import from here (keeps import paths consistent).
export { PulseCard, PulsePanel, UsersTable, CarriersTable };
